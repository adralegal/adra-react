import Typography from 'typography';

const typography = new Typography({
    baseFontSize: "24px",
    baseLineHeight: 1.0,
    headerFontFamily: [
        "Old Sans"
    ],
    bodyFontFamily: [
        "Old Sans"
    ]
});

export { typography as default };